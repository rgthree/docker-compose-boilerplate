# Access attempts that are invalid and have no mapped container.
[invalid]
enabled = true
chain = DOCKER-USER
logpath = /var/log/access.log
port = http,https
filter = invalid
maxretry = 3
bantime = 3600
findtime = 3600